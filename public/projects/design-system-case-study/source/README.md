# Design System Case Study Visual Sources

These files are the editable source files for the generated WebP visuals used by the case study.

## Editable generated visuals

- `cover.html` -> `../cover/cover.webp`
- `ai-workflow.html` -> `../solution/ai-workflow.webp`

`before-after-evolution.html` is retained as an editable reference. The case study now renders that comparison directly in HTML/CSS, so it no longer generates a WebP asset.

Open these HTML files in a browser or import them into Figma to edit the layout, text, colors, and shapes.

## Captured visual

- `figma-make-prototype-source.html` -> `../research/figma-make-prototype.webp`

This one came from the existing Figma Make site capture:

`https://orange-claim-01266597.figma.site`

It is not generated from local HTML artwork in the same way as the other three visuals.
